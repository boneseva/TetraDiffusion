# =============================================================================
# TetraDiffusion requirements.txt
# Generated from environment.yaml
#
# Install with:
#   pip install -r requirements.txt
#
# For PyTorch with CUDA 12.4 (matching environment.yaml pytorch=2.4.0 + cuda 12.4):
#   pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 \
#       --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
# =============================================================================

# ── PyTorch (install separately with the CUDA index-url above) ──────────────
# torch==2.4.0
# torchvision==0.19.0
# torchaudio==2.4.0

# ── Core scientific stack ────────────────────────────────────────────────────
numpy==1.26.4
pillow==10.4.0
networkx==3.3
sympy==1.12
mpmath==1.3.0

# ── Diffusion / training utilities ──────────────────────────────────────────
accelerate==0.33.0
ema-pytorch==0.5.3
einops==0.8.0

# ── 3D / mesh ────────────────────────────────────────────────────────────────
trimesh==4.4.4
scipy>=1.11.0          # EDT for voxel→SDF conversion in make_exemplar.py
rtree>=1.2.0           # spatial index backend required by trimesh raycasting

# ── Config / experiment tracking ─────────────────────────────────────────────
omegaconf==2.3.0
antlr4-python3-runtime==4.9.3
wandb

# ── HuggingFace ecosystem ────────────────────────────────────────────────────
huggingface-hub==0.24.5
safetensors==0.4.4
fsspec==2024.6.1

# ── Data handling ────────────────────────────────────────────────────────────
pandas==2.2.2
python-dateutil==2.9.0.post0
pytz==2024.1
tzdata==2024.1

# ── General utilities ─────────────────────────────────────────────────────────
tqdm==4.66.5
psutil==6.0.0
pyyaml==6.0.1
packaging==24.1
requests==2.32.3
urllib3==2.2.2
certifi==2024.7.4
charset-normalizer==3.3.2
idna==3.7
six==1.16.0
filelock==3.13.1
typing_extensions==4.11.0
jinja2==3.1.4
markupsafe==2.1.3

